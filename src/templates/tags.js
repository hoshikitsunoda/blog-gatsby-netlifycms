import React from "react"
import { Link, graphql } from "gatsby"
import Layout from "../components/layout"

const Tags = ({ data }) => {
  return (
    <Layout>
      <div>Tags</div>
    </Layout>
  )
}

export default Tags
